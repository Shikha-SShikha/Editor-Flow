services:
  - type: web
    name: editorial-assessment-flow
    runtime: static
    rootDir: .
    staticPublishPath: .
    buildCommand: echo "static site"
    headers:
      - path: /*
        name: Cache-Control
        value: no-cache
